import cl from "./Footer.module.scss"

const Footer = () => {
  return (
    <footer className={cl.footer}>
      <span className="_small">Open-source app. DoGether.</span>
    </footer>
  )
}

export default Footer
